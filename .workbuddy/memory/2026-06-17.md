# 2026-06-17 工作日志

## FAQ 分类改进任务 · 预检与开发步骤手册

- 任务来源：`1.md`，要求对客服 FAQ 自动分类脚本 `classifier.py` 做 Code Review + Prompt 改进 + 评估对比 + 工程化改进 + README。
- 附件：`classifier.py`（原始脚本）、`task1_prompt.md`（当前 prompt）、`task1_categories.md`（6 类标签定义）、`task1_test_samples.json`（30 条带 label 测试样本）。
- 预检发现原始代码至少 10 个问题：API Key 硬编码（严重）、OpenAI SDK 调用方式过时不兼容 ≥1.0（严重）、无 system prompt、prompt 未引用 categories 定义与冲突规则、输出无解析校验、无异常重试、batch 未读 label 字段导致无法算准确率、无 mock 模式（给的 key 是假的）、无 logging、prompt 硬编码在函数内。
- 关键判断：API key 是假的，任务允许 mock 模式，所以必须实现 mock 分类器才能跑评估；改进前后对比通过 baseline_mock（弱规则+不校验）vs 改进后 mock（细规则+校验）体现差异。
- 产出：`开发步骤手册.md`，包含 8 个步骤（步骤 0 项目初始化 → 步骤 7 提交 GitHub），每步含可直接粘进 Claude Code 的提示词、辅助手段、预期产出、注意事项。
- 工作模式：用户用 Claude Code 执行开发，主理人做预检+规划+逐步校验；用户每步把 Claude Code 输出回贴给主理人纠偏推进。
- 手册路径：`D:\lucan\Desktop\0107\开发步骤手册.md`
